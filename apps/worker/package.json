{
  "name": "@readaloud/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ReadAloud Cloudflare Worker — ElevenLabs TTS proxy + Workers AI summarizer. (Built in milestone 5.)",
  "scripts": {
    "typecheck": "echo \"worker: scaffolded in M5\" && exit 0",
    "build": "echo \"worker: scaffolded in M5\" && exit 0"
  }
}
