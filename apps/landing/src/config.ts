/**
 * Outbound link config — single source of truth.
 *
 * The extension isn't on the Chrome Web Store yet, so every "Add to Chrome" CTA
 * points at the GitHub repository (which has load-unpacked instructions in its
 * README). Once it's published, change `INSTALL_URL` to the Web Store listing
 * and every CTA updates with it.
 */
export const REPO_URL = 'https://github.com/gastonche/read-aloud';
export const INSTALL_URL = REPO_URL;
